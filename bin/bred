#!/usr/bin/env bash

while true; do
  if [ -n "$BRED_GDB" ] && [ "$BRED_GDB" -eq 1 ]; then
    npm run start-gdb -- $*
  else
    npm start -- $*
  fi
  CODE=$?
  echo "exit code: $CODE"
  if [ $CODE -eq 7 ]; then
    echo Relaunching...
    continue
  fi
  break
done
