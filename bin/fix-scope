#!/bin/bash

SCOPE=$1
if [[ $# -eq 2 ]]; then
  PREFIX=$2
else
  PREFIX="../"
fi
# will fail if there are :s in $PREFIX
cd lib/@${SCOPE}/ && find . -type f -name \*.js | xargs sed -i "s:^\(import .* from\) '\([^']\+\)'.*:\\1 '${PREFIX}\\2.js';:g"
